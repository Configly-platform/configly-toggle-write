spring:
  application:
    name: configly-toggle-write
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
  datasource:
    url: jdbc:postgresql://localhost:5433/configly_toggle_write
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  kafka:
    consumer:
      group-id: configly-toggle-write

topics: # Topic do konsumowania
  configly-structure-events: configly-structure-events

logging:
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr([${spring.application.name:-app}]){green} %clr([%X{correlationId:--}]){yellow} %clr([%X{actor:--}]){cyan} %clr(%-5level) -> %msg%n"

outbox:
  batch-size: 100
  pool-interval-ms: 10s
  initial-delay-ms: 0s
  attempt-limit: 3
  kafka:
    enabled: true
    bootstrap-servers: localhost:9092
    client-id: configly-toggle-write-client
    topics: # Topic do wytwarzania
      - name: configly-toggle-events
        partitions: 3
        replication-factor: 1
    props:
      spring.json.add.type.headers: true

#logging:
#  level:
#    org.springframework.kafka: DEBUG
#    org.apache.kafka.clients.consumer: DEBUG
server:
  port: 8082